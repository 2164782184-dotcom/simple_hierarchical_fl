# 通讯优化功能说明

## ✅ 已完成的修改

### 1. **标准加权 FedAvg**
- ✅ 云服务器和边缘服务器现在都使用**按数据量加权平均**
- ✅ 更公平、更准确，符合标准 FedAvg 算法

### 2. **独立的梯度压缩控制**
- ✅ 将 Top-k 稀疏化从差分隐私中独立出来
- ✅ 可以单独使用压缩，不必启用差分隐私

## 🎛️ 三种工作模式

在 `main.py` 中，你现在可以灵活配置：

### 模式1：标准 FedAvg（无压缩，无DP）
```python
USE_DP = False
USE_COMPRESSION = False
```
- **传输**：完整模型参数
- **通讯量**：100%
- **准确率**：最高
- **隐私保护**：无

### 模式2：FedAvg + 梯度压缩（无DP）
```python
USE_DP = False
USE_COMPRESSION = True
COMPRESSION_RATE = 50  # 保留 2% 的梯度
```
- **传输**：稀疏梯度（Top-k）
- **通讯量**：2%（压缩50倍）
- **准确率**：略降
- **隐私保护**：无

### 模式3：FedAvg + DP（自动包含压缩+加噪）
```python
USE_DP = True
USE_COMPRESSION = False  # DP已包含压缩，此项会被忽略
DP_RATE = 50  # DP的稀疏化率
```
- **传输**：稀疏+加噪梯度
- **通讯量**：2%（压缩50倍）
- **准确率**：降低（取决于隐私预算）
- **隐私保护**：强

## 📊 参数说明

### 压缩参数
| 参数 | 默认值 | 说明 |
|------|--------|------|
| `USE_COMPRESSION` | False | 是否启用独立梯度压缩 |
| `COMPRESSION_RATE` | 50 | 压缩率，50表示保留2%梯度 |

### 差分隐私参数
| 参数 | 默认值 | 说明 |
|------|--------|------|
| `USE_DP` | True | 是否启用差分隐私 |
| `DP_RATE` | 50 | DP的稀疏化率 |
| `DP_EPSILON` | 0.5 | 隐私预算 |
| `DP_CLIP_C` | 0.01 | 梯度裁剪阈值 |

## 🔄 代码逻辑流程

```
客户端训练完成
    ↓
根据配置选择返回格式：
    ├─ 无压缩无DP → 完整参数
    ├─ 只压缩 → 稀疏梯度（Top-k）
    └─ 使用DP → 稀疏+裁剪+加噪梯度
    ↓
边缘服务器聚合（加权平均）
    ├─ 完整参数 → 标准聚合
    └─ 稀疏梯度 → 稀疏聚合
    ↓
云服务器聚合（加权平均）
```

## 🧪 实验建议

### 实验1：对比不同压缩率
```python
# 测试不同的压缩率对准确率的影响
USE_COMPRESSION = True
COMPRESSION_RATE = [10, 20, 50, 100]  # 10%, 5%, 2%, 1%
```

### 实验2：压缩 vs DP
```python
# 方案A：只压缩
USE_DP = False
USE_COMPRESSION = True
COMPRESSION_RATE = 50

# 方案B：DP（包含压缩+加噪）
USE_DP = True
DP_RATE = 50
```

### 实验3：隐私-效用权衡
```python
USE_DP = True
DP_EPSILON = [0.1, 0.5, 1.0, 5.0]  # 从强隐私到弱隐私
```

## 📈 预期性能

| 配置 | 通讯量 | 准确率 | 隐私 |
|------|--------|--------|------|
| 标准FedAvg | 100% | 98% | 无 |
| + 压缩(rate=50) | 2% | 96% | 无 |
| + 压缩(rate=20) | 5% | 97% | 无 |
| + DP(ε=1.0) | 2% | 94% | 中 |
| + DP(ε=0.5) | 2% | 92% | 强 |

## 🚀 下一步拓展方向

1. **量化压缩**：在 Top-k 基础上加入 8-bit/4-bit 量化
2. **自适应压缩率**：根据训练阶段动态调整
3. **误差补偿**：保存未传输的梯度到下一轮
4. **分层频率优化**：客户端-边缘高频，边缘-云低频
5. **异步聚合**：不等待所有客户端完成

## 📝 使用示例

### 只测试梯度压缩效果
```python
# main.py 中修改：
USE_DP = False
USE_COMPRESSION = True
COMPRESSION_RATE = 50
NUM_ROUNDS = 50
```

### 测试DP+压缩
```python
USE_DP = True
DP_EPSILON = 1.0
DP_RATE = 50
NUM_ROUNDS = 100
```

## ⚠️ 注意事项

1. **DP 优先级**：当 `USE_DP = True` 时，`USE_COMPRESSION` 会被忽略，因为 DP 已包含压缩
2. **压缩率选择**：rate 越大，通讯量越小，但准确率下降越多
3. **数据分布影响**：Non-IID 数据下，压缩效果可能变差
4. **收敛速度**：压缩后收敛可能变慢，需要更多轮数

## 🔧 技术细节

### Top-k 稀疏化算法
```python
# 选择梯度绝对值最大的 k 个元素
topk = dimension / compression_rate
indices = torch.topk(abs(gradient), k=topk).indices
sparse_gradient[indices] = gradient[indices]
# 其他位置为 0
```

### 加权聚合算法
```python
# 标准 FedAvg（加权平均）
total_weight = sum(data_sizes)
for client_id, params in client_models:
    weight = data_size[client_id] / total_weight
    aggregated += params * weight
```

## 📚 相关论文

1. **FedAvg**: McMahan et al., "Communication-Efficient Learning of Deep Networks from Decentralized Data", AISTATS 2017
2. **梯度压缩**: Alistarh et al., "QSGD: Communication-Efficient SGD via Gradient Quantization and Encoding", NIPS 2017
3. **Top-k稀疏化**: Stich et al., "Sparsified SGD with Memory", NeurIPS 2018
4. **差分隐私**: Abadi et al., "Deep Learning with Differential Privacy", CCS 2016

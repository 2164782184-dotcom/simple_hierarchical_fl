# 通讯成本衡量指标说明

## 📊 主要衡量指标

### 1. **绝对通讯量（Absolute Communication Cost）**

#### 1.1 总通讯量（Total Communication）
```
总通讯量 = 上行通讯量 + 下行通讯量
单位：bits, KB, MB, GB
```

**用途**：评估整个训练过程的总通讯开销

#### 1.2 上行通讯量（Upload）
```
上行 = 客户端→边缘 + 边缘→云
```
- **客户端→边缘**：所有客户端上传模型/梯度
- **边缘→云**：所有边缘服务器上传聚合结果

#### 1.3 下行通讯量（Download）
```
下行 = 云→边缘 + 边缘→客户端
```
- **云→边缘**：云服务器广播全局模型
- **边缘→客户端**：边缘服务器广播模型

### 2. **平均通讯量（Average Communication Cost）**

#### 2.1 每轮通讯量
```
每轮通讯量 = 总通讯量 / 训练轮数
单位：MB/round
```

#### 2.2 每个客户端通讯量
```
每客户端 = 总通讯量 / (客户端数 × 训练轮数)
单位：MB/client/round
```

### 3. **压缩比（Compression Ratio）**

```
压缩比 = 基线通讯量 / 当前通讯量

例如：
- 基线（无压缩）：1000 MB
- 当前（Top-k=2%）：20 MB
- 压缩比 = 1000 / 20 = 50×
```

**用途**：评估压缩技术的效果

### 4. **通讯减少百分比（Communication Reduction）**

```
减少百分比 = (1 - 当前通讯量 / 基线通讯量) × 100%

例如：
- 基线：1000 MB
- 当前：20 MB
- 减少 = (1 - 20/1000) × 100% = 98%
```

### 5. **通讯轮数（Communication Rounds）**

```
总通讯次数 = (客户端数 + 边缘数) × 2 × 训练轮数
           = (上传次数 + 下载次数) × 训练轮数
```

**说明**：
- 每轮每个客户端上传1次、下载1次
- 每轮每个边缘服务器上传1次、下载1次

### 6. **通讯效率（Communication Efficiency）**

#### 6.1 准确率-通讯量比（Accuracy per MB）
```
效率 = 最终准确率 / 总通讯量(MB)

例如：
- 准确率 95%，通讯量 100 MB → 效率 = 0.95
- 准确率 93%，通讯量 20 MB → 效率 = 4.65 (更高效)
```

#### 6.2 收敛速度（Convergence per MB）
```
收敛效率 = 达到目标准确率的通讯量

例如：
- 方案A：90%准确率，需要 500 MB
- 方案B：90%准确率，需要 50 MB (更高效)
```

## 🧮 计算公式详解

### 完整模型参数大小

```python
# float32 参数
模型大小(bits) = 参数数量 × 32
模型大小(MB) = 参数数量 × 32 / (8 × 1024 × 1024)
            = 参数数量 × 4 / (1024 × 1024)
            ≈ 参数数量 / 262144

例如：
- 参数数量 = 1,000,000 (1M)
- 大小 = 1,000,000 × 4 bytes = 4 MB
```

### 稀疏梯度大小

```python
# Top-k 稀疏化
非零元素数量 = 总参数数 / 压缩率
稀疏大小(bits) = 非零元素数量 × 32  # 梯度值
               + 非零元素数量 × 32  # 索引
               = 非零元素数量 × 64

例如：
- 总参数 = 1,000,000
- 压缩率 = 50 (保留2%)
- 非零元素 = 20,000
- 大小 = 20,000 × 64 bits = 160 KB
- 压缩比 = 4 MB / 0.16 MB = 25×
```

### 分层通讯量计算

```python
# 一轮训练的通讯量

# 1. 云→边缘（下行）
云到边缘 = 模型大小 × 边缘数

# 2. 边缘→客户端（下行）
边缘到客户端 = 模型大小 × 客户端总数

# 3. 客户端→边缘（上行）
客户端到边缘 = (完整模型 或 稀疏梯度) × 客户端总数

# 4. 边缘→云（上行）
边缘到云 = 聚合后模型大小 × 边缘数

# 总计
每轮通讯量 = (1) + (2) + (3) + (4)
```

## 📈 实际例子

### 场景：MNIST + SimpleCNN

**模型参数**：
- Conv1: 1×32×5×5 = 800
- Conv2: 32×64×5×5 = 51,200
- FC1: 3136×512 = 1,605,632
- FC2: 512×10 = 5,120
- **总计**: ~1,662,752 参数 ≈ **6.35 MB**（float32）

**配置**：
- 客户端数：50
- 边缘服务器：5
- 训练轮数：100

### 方案对比

#### 方案1：标准 FedAvg（无压缩）

```
每轮通讯量：
- 云→边缘: 6.35 MB × 5 = 31.75 MB
- 边缘→客户端: 6.35 MB × 50 = 317.5 MB
- 客户端→边缘: 6.35 MB × 50 = 317.5 MB
- 边缘→云: 6.35 MB × 5 = 31.75 MB
- 小计: 698.5 MB/round

100轮总计: 69,850 MB ≈ 68.2 GB
```

#### 方案2：Top-k 压缩（rate=50，保留2%）

```
稀疏梯度大小 = 6.35 MB × 0.02 × 2 = 0.254 MB

每轮通讯量：
- 云→边缘: 6.35 MB × 5 = 31.75 MB
- 边缘→客户端: 6.35 MB × 50 = 317.5 MB
- 客户端→边缘: 0.254 MB × 50 = 12.7 MB (↓)
- 边缘→云: 6.35 MB × 5 = 31.75 MB
- 小计: 393.7 MB/round

100轮总计: 39,370 MB ≈ 38.4 GB

压缩比: 68.2 / 38.4 = 1.78×
减少: 43.6%
```

#### 方案3：Top-k 压缩（rate=100，保留1%）

```
稀疏梯度大小 = 6.35 MB × 0.01 × 2 = 0.127 MB

每轮通讯量：
- 云→边缘: 31.75 MB
- 边缘→客户端: 317.5 MB
- 客户端→边缘: 0.127 MB × 50 = 6.35 MB (↓↓)
- 边缘→云: 31.75 MB
- 小计: 387.35 MB/round

100轮总计: 38,735 MB ≈ 37.8 GB

压缩比: 68.2 / 37.8 = 1.80×
减少: 44.6%
```

## 🎯 实验对比指标

### 完整对比表

| 方案 | 每轮(MB) | 总计(GB) | 压缩比 | 准确率 | 效率(Acc/GB) |
|------|---------|---------|--------|--------|-------------|
| 标准FedAvg | 698.5 | 68.2 | 1.0× | 98% | 1.44 |
| + Top-k(2%) | 393.7 | 38.4 | 1.78× | 96% | 2.50 ↑ |
| + Top-k(1%) | 387.4 | 37.8 | 1.80× | 95% | 2.51 ↑ |
| + DP(ε=1.0) | 393.7 | 38.4 | 1.78× | 94% | 2.45 |

**结论**：Top-k(2%) 在保持高准确率的同时，通讯效率提升 73%

## 📊 可视化建议

### 图1：通讯量随训练轮次变化
```python
plt.plot(rounds, cumulative_communication_mb)
plt.xlabel('Training Rounds')
plt.ylabel('Cumulative Communication (MB)')
```

### 图2：压缩率 vs 准确率
```python
plt.scatter(compression_ratios, accuracies)
plt.xlabel('Compression Ratio')
plt.ylabel('Test Accuracy (%)')
```

### 图3：通讯效率对比
```python
methods = ['FedAvg', 'Top-k 2%', 'Top-k 1%', 'DP']
efficiency = [acc / comm for acc, comm in zip(accuracies, communications)]
plt.bar(methods, efficiency)
```

## 🔧 在代码中使用

```python
from communication_metrics import CommunicationMetrics

# 初始化
comm_metrics = CommunicationMetrics()

# 记录通讯
comm_metrics.record_client_upload(client_models)
comm_metrics.record_edge_upload(edge_models)
comm_metrics.record_cloud_broadcast(num_edges, model)
comm_metrics.record_edge_broadcast(num_clients, model)

# 每轮结束
comm_metrics.record_round_end()

# 获取统计
summary = comm_metrics.get_summary()
print(f"Total: {summary['total_communication_mb']:.2f} MB")

# 打印报告
comm_metrics.print_summary()

# 计算压缩比
baseline = CommunicationMetrics()  # 无压缩的基线
ratio, reduction = comm_metrics.calculate_compression_ratio(baseline)
print(f"Compression: {ratio:.2f}×, Reduction: {reduction:.1f}%")
```

## 📝 论文中常用的指标

1. **Total Communication Cost (TCC)**: 总通讯量（GB）
2. **Communication Rounds (CR)**: 通讯轮数
3. **Communication per Round (CPR)**: 每轮通讯量（MB）
4. **Compression Ratio (γ)**: 压缩比
5. **Communication Efficiency (η)**: 通讯效率 = Accuracy / TCC
6. **Convergence Speed**: 达到目标准确率所需的通讯量

## 🎓 相关工作对比

典型论文中的通讯量报告：

```
Method          | Comm. Cost | Accuracy | Efficiency
----------------|------------|----------|------------
FedAvg          | 100%       | 98.0%    | 1.0×
FedProx         | 100%       | 98.2%    | 1.0×
Top-k (10%)     | 20%        | 97.5%    | 4.9×
Quantization    | 25%        | 97.8%    | 3.9×
Our Method      | 15%        | 97.3%    | 6.5× ✓
```
